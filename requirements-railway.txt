# Minimal dependencies for Railway deployment without numpy/pandas
# Core dependencies
python-dotenv==1.0.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Supabase (pin exact versions to avoid proxy parameter issues)
supabase==2.8.1
postgrest==0.17.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0

# Web scraping (pin exact version to avoid proxy parameter issues)
httpx==0.27.2
beautifulsoup4==4.12.3
lxml==5.1.0
firecrawl-py>=0.0.14
# Native scraping dependencies
aiohttp==3.9.3
aiofiles==23.2.1
fake-useragent==1.4.0
cchardet==2.1.7
html5lib==1.1

# API framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Task queue (compatible versions)
celery[redis]==5.3.4
redis<5.0.0

# Monitoring
prometheus-client==0.19.0

# Basic text processing (no numpy dependency)
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.21.0
rapidfuzz>=3.0.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0

# Development
black==23.12.1
flake8==7.0.0
mypy==1.8.0